package Sweets;

public interface forSweets {
    void add(Sweets s);
    void del();
    float genWeight();
    float genPrice();
    String genInfo();

    void maxWeight(float max);
    void optimization();
}
